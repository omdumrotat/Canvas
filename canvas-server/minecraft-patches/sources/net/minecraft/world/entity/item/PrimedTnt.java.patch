--- a/net/minecraft/world/entity/item/PrimedTnt.java
+++ b/net/minecraft/world/entity/item/PrimedTnt.java
@@ -59,6 +_,15 @@
     private boolean usedPortal;
     public float explosionPower = 4.0F;
     public boolean isIncendiary = false; // CraftBukkit
+    // Canvas start - merge tnt logic
+    float setPower = 4.0F;
+
+    public void addPower() {
+        if (setPower < 16.0F) {
+            setPower += 0.1F;
+        }
+    }
+    // Canvas end
 
     public PrimedTnt(EntityType<? extends PrimedTnt> entityType, Level level) {
         super(entityType, level);
@@ -100,7 +_,7 @@
 
     @Override
     public void tick() {
-        if (this.level().spigotConfig.maxTntTicksPerTick > 0 && ++this.level().spigotConfig.currentPrimedTnt > this.level().spigotConfig.maxTntTicksPerTick) { return; } // Spigot
+        if (this.level().spigotConfig.maxTntTicksPerTick > 0 && ++io.canvasmc.canvas.region.ServerRegions.getTickData(this.level().level()).currentPrimedTnt > this.level().spigotConfig.maxTntTicksPerTick) { return; } // Spigot // Canvas - Threaded Regions
         this.handlePortal();
         this.applyGravity();
         this.move(MoverType.SELF, this.getDeltaMovement());
@@ -148,6 +_,13 @@
                 return;
             }
             // CraftBukkit end
+            // Canvas start - merge tnt logic
+            // The reason we dont add to HEAD is so we respect the plugin event.
+            if (io.canvasmc.canvas.Config.INSTANCE.blocks.tnt.mergeTntLogic) {
+                this.level().explode(this, Explosion.getDefaultDamageSource(this.level(), this), this.usedPortal ? USED_PORTAL_DAMAGE_CALCULATOR : null, this.getX(), this.getY(0.0625), this.getZ(), this.setPower, false, Level.ExplosionInteraction.TNT);
+                return;
+            }
+            // Canvas end
             this.level()
                 .explode(
                     this,
