--- a/net/minecraft/world/entity/projectile/Projectile.java
+++ b/net/minecraft/world/entity/projectile/Projectile.java
@@ -49,6 +_,42 @@
     Projectile(EntityType<? extends Projectile> entityType, Level level) {
         super(entityType, level);
     }
+    // Pufferfish start
+
+    private static int loadedThisTick = 0;
+    private static long loadedTick; // Canvas - region threading
+    private int loadedLifetime = 0;
+
+    @Override
+    public void setPos(double x, double y, double z) {
+        // Canvas start - region threading
+        if (!ca.spottedleaf.moonrise.common.util.TickThread.isTickThreadFor(this)) {
+            super.setPos(x, y, z);
+            return;
+        }
+        // Canvas end - region threading
+        long currentTick = this.level().getCurrentWorldData().regionData.getCurrentTick(); // Canvas - region threading
+        if (loadedTick != currentTick) {
+            loadedTick = currentTick;
+            loadedThisTick = 0;
+        }
+        int previousX = Mth.floor(this.getX()) >> 4, previousZ = Mth.floor(this.getZ()) >> 4;
+        int newX = Mth.floor(x) >> 4, newZ = Mth.floor(z) >> 4;
+        if (previousX != newX || previousZ != newZ) {
+            boolean isLoaded = ((net.minecraft.server.level.ServerChunkCache) this.level().getChunkSource()).getChunkAtIfLoadedImmediately(newX, newZ) != null;
+            if (!isLoaded) {
+                if (Projectile.loadedThisTick > io.canvasmc.canvas.Config.INSTANCE.projectiles.maxProjectileLoadsPerTick) {
+                    if (++this.loadedLifetime > io.canvasmc.canvas.Config.INSTANCE.projectiles.maxProjectileLoadsPerProjectile) {
+                        this.discard();
+                    }
+                    return;
+                }
+                Projectile.loadedThisTick++;
+            }
+        }
+        super.setPos(x, y, z);
+    }
+    // Pufferfish end
 
     protected void setOwner(@Nullable EntityReference<Entity> owner) {
         this.owner = owner;
