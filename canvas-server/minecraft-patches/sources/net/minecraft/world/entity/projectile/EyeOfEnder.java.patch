--- a/net/minecraft/world/entity/projectile/EyeOfEnder.java
+++ b/net/minecraft/world/entity/projectile/EyeOfEnder.java
@@ -29,6 +_,7 @@
     public double tz;
     public int life;
     public boolean surviveAfterDeath;
+    public boolean asyncLocator$locateTaskOngoing = false; // Canvas - Asynchronous locator
 
     public EyeOfEnder(EntityType<? extends EyeOfEnder> entityType, Level level) {
         super(entityType, level);
@@ -116,6 +_,11 @@
     @Override
     public void tick() {
         super.tick();
+        // Canvas start - Asynchronous locator
+        if (io.canvasmc.canvas.Config.INSTANCE.asyncLocator.enabled && this.asyncLocator$locateTaskOngoing) {
+            return;
+        }
+        // Canvas end - Asynchronous locator
         Vec3 deltaMovement = this.getDeltaMovement();
         double d = this.getX() + deltaMovement.x;
         double d1 = this.getY() + deltaMovement.y;
