--- a/net/minecraft/world/entity/monster/Vex.java
+++ b/net/minecraft/world/entity/monster/Vex.java
@@ -407,7 +_,7 @@
         @Override
         public void tick() {
             BlockPos boundOrigin = Vex.this.getBoundOrigin();
-            if (boundOrigin == null) {
+            if (boundOrigin == null || !io.canvasmc.canvas.region.ServerRegions.isTickThreadFor(Vex.this.level(), boundOrigin)) { // Canvas - Threaded Regions
                 boundOrigin = Vex.this.blockPosition();
             }
 
